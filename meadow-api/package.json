{
  "name": "@meadow/api",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "lint": "prettier --check . && eslint .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "hono": "^4.7.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@meadow/config": "workspace:*"
  }
}
